export interface ProjectModel {
  name: string;
  abbreviation: string;
  description: string;
  initialRisk: Date;
}

export interface ProjectDetailsModel {
  projectId: string;
  name: string;
  abbreviation: string;
  description: string;
  initialRisk: Date;
}
