export interface TeamModel {
  name: string;
  membersCount: number;
  projectId: string;
}

export interface TeamDetailsModel {
  teamId: string;
  name: string;
  membersCount: number;
  projectId: string;
}
